// particles.js
// particles were removed — keep a minimal stub to avoid import errors
export const particles = [];
export function createParticles(/* num, canvasWidth, canvasHeight */) {
    // no-op
}
